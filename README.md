# 恋爱风格深度测评

## 快速开始

1. 编辑 `config.json` — 修改题目、维度、等级
2. 编辑 `report-templates.js` — 修改报告内容
3. 浏览器打开 `index.html` 预览
4. 部署到 GitHub Pages / 妙搭

## 部署

```bash
# GitHub Pages
git add . && git commit -m "init" && git push

# 妙搭
lark-cli apps +html-publish --app-id <app_id> --path .
```
